import 'package:flutter/material.dart';

class BannerBoostOrdersScreen extends StatelessWidget {
  const BannerBoostOrdersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

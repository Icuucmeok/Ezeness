import 'package:flutter/material.dart';

class PostBoostOrdersScreen extends StatelessWidget {
  const PostBoostOrdersScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
